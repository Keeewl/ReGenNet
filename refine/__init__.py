"""New Stage2 implementation namespace.

This package is intentionally separate from `stage2_old/`.
Do not runtime import `stage2_old.*`, `model.contact.*`, or `model.crefine.*`
from code placed under `refine/`.
"""
